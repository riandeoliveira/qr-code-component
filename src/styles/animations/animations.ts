export const fadeIn = () => {
  return `@keyframes fadeIn {
      from {
          opacity: 0;
      }

      to {
          opacity: 1;
      }
  }`;
};

export const increase = () => {
  return `@keyframes increase {
      to {
          width: 100%;
      }
  }`;
};
